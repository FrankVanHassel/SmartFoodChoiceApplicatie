<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SFC_App.Views.AboutPage"
             xmlns:vm="clr-namespace:SFC_App.ViewModels"
             xmlns:local="clr-namespace:SFC_App"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
            <Color x:Key="AppGreen">#56FF00</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid BackgroundColor="#56FF00">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                <Image Source="{local:ImageResource SFC_App.Images.banaan-vierkant.jpg}" ></Image>
            </StackLayout>
        </StackLayout>
    </Grid>
       
</ContentPage>
